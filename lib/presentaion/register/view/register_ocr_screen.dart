import 'package:flutter/material.dart';

class RegisterOcrScreen extends StatelessWidget {
  const RegisterOcrScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('카메라 화면')),
    );
  }
}
